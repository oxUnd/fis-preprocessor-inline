fis-preprocessor-inline
================

###install

```bash
$ npm install -g fis-preprocessor-inline
```

###config

```javascript
fis.config.merge({
    ...
    modules: {
        preprocessor: {
            tpl: 'inline'
        }
    }
});
````
